@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>GymBuddy — Exercise Library</title>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;0,9..40,700;1,9..40,300&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet" />
  <style>
    /* ══ PALETTE ════════════════════════════════════════════════
       B2B9C1  D8DCDF  FF7200  FF8B10  FF9942
    ═══════════════════════════════════════════════════════════ */
    :root {
      --bg:        #1E1E1E;
      --surf:      #252525;
      --card:      #2a2a2a;
      --card2:     #313131;
      --border:    #3a3a3a;
      --border2:   #B2B9C1;
      --muted:     #7a8490;
      --dim:       #9da6b0;
      --text:      #c8cdd4;
      --light:     #f0f2f4;
      --white:     #ffffff;

      --gray1:     #B2B9C1;
      --gray2:     #D8DCDF;

      --orange:    #FF7200;
      --orange2:   #FF8B10;
      --orange3:   #FF9942;
      --og:        rgba(255,114,0,.10);
      --og2:       rgba(255,114,0,.18);
      --og3:       rgba(255,114,0,.32);

      --r:         10px;
      --r2:        16px;
      --display:   'Bebas Neue',sans-serif;
      --body:      'DM Sans',sans-serif;
      --mono:      'DM Mono',monospace;
    }

    *,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
    html{scroll-behavior:smooth}
    body{background:var(--bg);color:var(--text);font-family:var(--body);font-size:14px;line-height:1.6;min-height:100vh;overflow-x:hidden}
    ::-webkit-scrollbar{width:5px}
    ::-webkit-scrollbar-track{background:var(--bg)}
    ::-webkit-scrollbar-thumb{background:#444;border-radius:3px}
    ::-webkit-scrollbar-thumb:hover{background:var(--orange)}
    button{font-family:var(--body)}

    /* ══ PAGE SYSTEM ════════════════════════════════════════════ */
    #introPage{display:flex;flex-direction:column;min-height:100vh}
    #appPage{display:none;flex-direction:column}
    #appPage.active{display:flex}

    /* ════════════════════════════════════════════════════════════
       INTRO PAGE
    ═════════════════════════════════════════════════════════════ */

    /* Fixed nav */
    .intro-topbar{
      position:fixed;top:0;left:0;right:0;z-index:100;
      display:flex;align-items:center;justify-content:space-between;
      padding:0 56px;height:60px;
      background:rgba(30,30,30,.92);
      backdrop-filter:blur(16px);
      border-bottom:1px solid var(--border);
    }
    .intro-logo{
      font-family:var(--display);font-size:22px;letter-spacing:2px;
      color:var(--light);display:flex;align-items:center;gap:9px;
    }
    .intro-logo-pip{width:9px;height:9px;border-radius:50%;background:var(--orange)}
    .intro-logo em{color:var(--orange);font-style:normal}
    .nav-link{
      font-family:var(--mono);font-size:11px;letter-spacing:1.5px;text-transform:uppercase;
      color:var(--dim);background:none;
      border:1px solid var(--border2);border-radius:var(--r);
      padding:7px 16px;cursor:pointer;transition:all .15s;
    }
    .nav-link:hover{border-color:var(--orange);color:var(--orange);background:var(--og)}

    /* Hero — compact split */
    .intro-hero{
      margin-top:60px;
      display:grid;
      grid-template-columns:1fr 340px;
      height:calc(100vh - 60px);
      max-height:calc(100vh - 60px);
      overflow:hidden;
    }

    /* Left text panel — content pinned to bottom via absolute */
    .intro-left{
      position:relative;
      background:var(--surf);
      border-right:1px solid var(--border);
      overflow:hidden;
    }
    .intro-left-content{
      position:absolute;
      bottom:0;left:0;right:0;
      padding:0 52px 32px;
    }
    .intro-eyebrow{
      font-family:var(--mono);font-size:10px;letter-spacing:3px;text-transform:uppercase;
      color:var(--orange);display:flex;align-items:center;gap:10px;margin-bottom:10px;
    }
    .intro-eyebrow::before{content:'';width:24px;height:1px;background:var(--orange)}
    .intro-h1{
      font-family:var(--display);
      font-size:clamp(36px,4.2vw,58px);
      letter-spacing:2px;line-height:.92;
      color:var(--light);margin-bottom:14px;
    }
    .intro-h1 em{color:var(--orange);font-style:normal;display:block}
    .intro-sub{
      font-size:13px;color:var(--dim);line-height:1.6;
      max-width:440px;margin-bottom:18px;font-weight:300;
    }
    .intro-sub strong{color:var(--text);font-weight:500}

    /* Stats */
    .intro-stats{display:flex;gap:10px;margin-bottom:18px;flex-wrap:wrap}
    .istat{display:flex;flex-direction:column;gap:1px;padding:10px 14px;background:var(--card2);border:1px solid var(--border);border-radius:var(--r);min-width:80px}
    .istat-num{font-family:var(--display);font-size:24px;letter-spacing:1px;color:var(--light);line-height:1}
    .istat-num span{color:var(--orange)}
    .istat-label{font-family:var(--mono);font-size:8.5px;letter-spacing:2px;text-transform:uppercase;color:var(--muted)}

    /* CTA */
    .cta-btn{
      display:inline-flex;align-items:center;gap:10px;
      background:var(--orange);color:#fff;
      font-family:var(--display);font-size:16px;letter-spacing:1.5px;text-transform:uppercase;
      padding:12px 24px;border-radius:var(--r);border:none;cursor:pointer;
      transition:all .22s;box-shadow:0 4px 18px var(--og3);width:fit-content;
    }
    .cta-btn:hover{background:var(--orange2);transform:translateY(-2px);box-shadow:0 8px 24px var(--og3)}
    .cta-btn svg{width:16px;height:16px;transition:transform .2s}
    .cta-btn:hover svg{transform:translateX(4px)}
    .intro-note{margin-top:10px;font-size:11px;color:var(--muted);display:flex;align-items:center;gap:6px}
    .intro-note svg{width:12px;height:12px;color:var(--orange)}

    /* Right photo column — 2-col mosaic, controlled height */
    .intro-right{
      position:relative;overflow:hidden;
      display:grid;
      grid-template-columns:1fr 1fr;
      grid-template-rows:repeat(3,1fr);
      gap:2px;
      background:var(--card);
      margin-left:-60px;
      padding-left:60px;
    }
    .intro-right::before{
      content:'';position:absolute;inset:0;z-index:2;pointer-events:none;
      background:linear-gradient(to right,var(--surf) 0%,transparent 30%),
                 linear-gradient(to bottom,rgba(30,30,30,.6) 0%,transparent 12%,transparent 88%,rgba(30,30,30,.6) 100%);
    }
    .mosaic-img{overflow:hidden;position:relative}
    .mosaic-img img{
      width:100%;height:100%;object-fit:cover;
      filter:grayscale(20%) brightness(.88) saturate(.9);
      animation:kb 14s ease-in-out infinite alternate;
    }
    .mosaic-img:nth-child(1){grid-row:1/3}
    .mosaic-img:nth-child(2) img{animation-delay:-4s}
    .mosaic-img:nth-child(3) img{animation-delay:-8s}
    .mosaic-img:nth-child(4) img{animation-delay:-2s}
    .mosaic-img:nth-child(5) img{animation-delay:-6s}
    @@keyframes kb{from{transform:scale(1)}to{transform:scale(1.08) translate(-1%,-1%)}}

    /* Feature strip */
    .intro-features{
      background:var(--surf);
      border-top:1px solid var(--border);
      display:grid;grid-template-columns:repeat(4,1fr);
    }
    .feat{
      padding:30px 36px;border-right:1px solid var(--border);
      display:flex;flex-direction:column;gap:10px;
    }
    .feat:last-child{border-right:none}
    .feat-icon{
      width:40px;height:40px;border-radius:9px;
      background:var(--og);border:1px solid rgba(255,114,0,.2);
      display:flex;align-items:center;justify-content:center;color:var(--orange);
    }
    .feat-icon svg{width:20px;height:20px}
    .feat-title{font-family:var(--display);font-size:18px;letter-spacing:.5px;color:var(--light)}
    .feat-desc{font-size:12.5px;color:var(--muted);line-height:1.6}

    /* ════════════════════════════════════════════════════════════
       APP PAGE
    ═════════════════════════════════════════════════════════════ */

    .app-shell{
      display:grid;
      grid-template-columns:256px 1fr;
      grid-template-rows:60px 1fr;
      min-height:100vh;
    }

    /* App topbar */
    .app-topbar{
      grid-column:1/-1;
      display:flex;align-items:center;gap:16px;
      padding:0 22px;
      background:rgba(30,30,30,.95);
      backdrop-filter:blur(16px);
      border-bottom:1px solid var(--border);
      position:sticky;top:0;z-index:200;
    }
    .back-btn{
      display:flex;align-items:center;gap:5px;
      background:none;border:1px solid var(--border);color:var(--muted);
      border-radius:var(--r);padding:6px 13px;font-size:12px;cursor:pointer;
      transition:all .15s;font-family:var(--mono);letter-spacing:.5px;
    }
    .back-btn:hover{border-color:var(--orange);color:var(--orange)}
    .back-btn svg{width:13px;height:13px}
    .app-logo{font-family:var(--display);font-size:20px;letter-spacing:2px;color:var(--light)}
    .app-logo em{color:var(--orange);font-style:normal}
    .search-wrap{flex:1;max-width:380px;position:relative}
    .search-wrap input{
      width:100%;background:var(--card2);border:1px solid var(--border);
      border-radius:999px;padding:8px 34px 8px 38px;
      color:var(--text);font-family:var(--body);font-size:13.5px;outline:none;
      transition:border-color .2s,box-shadow .2s;
    }
    .search-wrap input::placeholder{color:var(--muted)}
    .search-wrap input:focus{border-color:var(--orange);box-shadow:0 0 0 3px var(--og)}
    .s-ico{position:absolute;left:12px;top:50%;transform:translateY(-50%);color:var(--muted);pointer-events:none}
    .s-clr{position:absolute;right:11px;top:50%;transform:translateY(-50%);background:none;border:none;color:var(--muted);cursor:pointer;font-size:17px;display:none;line-height:1}
    .search-wrap input:not(:placeholder-shown)~.s-clr{display:block}
    .app-end{margin-left:auto}
    .cnt-badge{font-family:var(--mono);font-size:12px;color:var(--muted);background:var(--card2);border:1px solid var(--border);border-radius:999px;padding:4px 13px}
    .cnt-badge strong{color:var(--orange)}

    /* Sidebar */
    .sidebar{
      background:var(--surf);border-right:1px solid var(--border);
      overflow-y:auto;padding:18px 0;
    }
    .sb-label{
      font-family:var(--mono);font-size:9.5px;letter-spacing:2.5px;
      text-transform:uppercase;color:var(--muted);padding:0 16px 9px;
    }
    .nav-item{
      display:flex;align-items:center;gap:11px;
      padding:8px 16px;cursor:pointer;
      border-left:2px solid transparent;
      transition:background .14s,border-color .14s;
    }
    .nav-item:hover{background:var(--og)}
    .nav-item.active{background:var(--og);border-left-color:var(--orange)}
    .nav-thumb{
      width:40px;height:40px;border-radius:8px;overflow:hidden;
      flex-shrink:0;border:1px solid var(--border);
    }
    .nav-thumb img{width:100%;height:100%;object-fit:cover;filter:grayscale(30%);transition:filter .2s}
    .nav-item.active .nav-thumb img{filter:grayscale(0%)}
    .nav-item.active .nav-thumb{border-color:rgba(255,114,0,.4)}
    .nav-info{flex:1;min-width:0}
    .nav-name{font-size:13px;font-weight:500;color:var(--text);display:block;line-height:1.2}
    .nav-sub{font-size:11px;color:var(--muted);display:block;margin-top:1px}
    .nav-item.active .nav-name{color:var(--orange)}
    .sb-rule{height:1px;background:var(--border);margin:14px 16px}

    /* Difficulty */
    .diff-wrap{padding:0 16px}
    .diff-pill{
      display:flex;align-items:center;gap:8px;width:100%;
      background:var(--card2);border:1px solid var(--border);
      border-radius:var(--r);padding:7px 11px;cursor:pointer;
      font-size:12.5px;color:var(--dim);margin-bottom:5px;
      transition:all .14s;text-align:left;
    }
    .diff-pill:hover{border-color:var(--border2);color:var(--text)}
    .diff-pill.active{border-color:var(--orange);color:var(--orange);background:var(--og)}
    .dd{width:7px;height:7px;border-radius:50%;flex-shrink:0}
    .dd.a{background:var(--gray1)}.dd.b{background:#16a34a}.dd.i{background:var(--orange2)}.dd.e{background:#dc2626}

    /* Main panel */
    .main-panel{overflow-y:auto;display:flex;flex-direction:column;background:var(--bg)}

    /* Hero header with photo */
    .main-hero{position:relative;overflow:hidden;border-bottom:1px solid var(--border)}
    .hero-img-strip{height:200px;position:relative;overflow:hidden}
    .hero-img-strip img{width:100%;height:100%;object-fit:cover;filter:brightness(.55) saturate(.7)}
    .hero-img-strip::after{
      content:'';position:absolute;inset:0;
      background:linear-gradient(to bottom,rgba(30,30,30,.1) 0%,#1E1E1E 100%);
    }
    .hero-content{position:absolute;bottom:0;left:0;right:0;padding:0 32px 22px;z-index:2}
    .hero-eyebrow{
      font-family:var(--mono);font-size:10px;letter-spacing:2.5px;text-transform:uppercase;
      color:var(--orange);margin-bottom:6px;display:flex;align-items:center;gap:8px;
    }
    .hero-eyebrow::before{content:'';width:18px;height:1px;background:var(--orange)}
    .hero-title{
      font-family:var(--display);font-size:clamp(32px,4.5vw,52px);
      letter-spacing:2px;color:var(--white);line-height:.95;margin-bottom:6px;
    }
    .hero-title em{color:var(--orange3);font-style:normal}
    .hero-meta{font-size:12.5px;color:rgba(255,255,255,.55)}

    /* Type chips */
    .chip-row{
      display:flex;align-items:center;gap:7px;
      padding:10px 32px;border-bottom:1px solid var(--border);
      background:var(--surf);overflow-x:auto;scrollbar-width:none;
    }
    .chip-row::-webkit-scrollbar{display:none}
    .chip-row-lbl{font-family:var(--mono);font-size:9.5px;letter-spacing:2px;text-transform:uppercase;color:var(--muted);white-space:nowrap;flex-shrink:0}
    .chip{
      padding:4px 13px;border-radius:999px;border:1px solid var(--border);
      background:none;color:var(--muted);font-size:12px;cursor:pointer;
      transition:all .14s;white-space:nowrap;flex-shrink:0;text-transform:capitalize;
    }
    .chip:hover{border-color:var(--orange);color:var(--orange);background:var(--og)}
    .chip.active{background:var(--orange);border-color:var(--orange);color:#fff;font-weight:600}

    /* Content */
    .content{padding:24px 32px;flex:1}
    .ex-grid{display:grid;grid-template-columns:repeat(auto-fill,minmax(300px,1fr));gap:18px}

    /* Card */
    .ex-card{
      background:var(--card);border:1px solid var(--border);
      border-radius:var(--r2);overflow:hidden;cursor:pointer;
      display:flex;flex-direction:column;
      transition:border-color .2s,box-shadow .2s,transform .2s;
      animation:riseIn .38s cubic-bezier(.22,.68,0,1.2) both;
    }
    .ex-card:hover{
      border-color:var(--orange);
      box-shadow:0 8px 28px rgba(255,114,0,.14),0 0 0 1px var(--orange);
      transform:translateY(-3px);
    }
    @@keyframes riseIn{from{opacity:0;transform:translateY(16px) scale(.97)}to{opacity:1;transform:translateY(0) scale(1)}}

    .card-img-wrap{height:180px;position:relative;overflow:hidden;background:var(--card2);flex-shrink:0}
    .card-img-wrap img{
      width:100%;height:100%;object-fit:cover;
      filter:grayscale(20%) brightness(.9) saturate(.85);
      transition:filter .3s,transform .4s;
    }
    .ex-card:hover .card-img-wrap img{filter:grayscale(0%) brightness(.95) saturate(1);transform:scale(1.05)}
    .card-img-wrap::after{
      content:'';position:absolute;bottom:0;left:0;right:0;height:70px;
      background:linear-gradient(to top,var(--card) 0%,transparent 100%);
      pointer-events:none;
    }

    /* Orange top accent line */
    .card-accent{height:3px;background:linear-gradient(90deg,var(--orange) 0%,var(--orange3) 100%);flex-shrink:0}

    .diff-badge{
      position:absolute;top:10px;left:10px;z-index:2;
      font-family:var(--mono);font-size:9.5px;letter-spacing:1.5px;text-transform:uppercase;
      padding:3px 9px;border-radius:999px;border:1px solid;backdrop-filter:blur(6px);
    }
    .diff-badge.beginner{color:#16a34a;border-color:#16a34a;background:rgba(22,163,74,.12)}
    .diff-badge.intermediate{color:var(--orange2);border-color:var(--orange2);background:rgba(255,139,16,.12)}
    .diff-badge.expert{color:#dc2626;border-color:#dc2626;background:rgba(220,38,38,.12)}
    .type-badge{
      position:absolute;top:10px;right:10px;z-index:2;
      font-size:9.5px;color:var(--dim);
      background:rgba(30,30,30,.82);border:1px solid var(--border);
      border-radius:999px;padding:3px 9px;text-transform:capitalize;letter-spacing:.5px;
    }

    .card-body{padding:13px 16px 15px;flex:1;display:flex;flex-direction:column;gap:7px}
    .card-name{font-family:var(--display);font-size:19px;letter-spacing:.8px;color:var(--light);line-height:1.1;text-transform:capitalize}
    .card-muscle{display:flex;align-items:center;gap:5px}
    .muscle-pip{width:5px;height:5px;border-radius:50%;background:var(--orange);flex-shrink:0}
    .card-muscle span{font-size:11.5px;color:var(--orange);font-weight:500;text-transform:capitalize}
    .card-desc{font-size:12.5px;color:var(--muted);line-height:1.55;display:-webkit-box;-webkit-line-clamp:3;-webkit-box-orient:vertical;overflow:hidden;flex:1}

    .card-foot{
      display:flex;align-items:center;justify-content:space-between;
      padding:10px 16px;border-top:1px solid var(--border);
      background:var(--card2);
    }
    .card-equip svg{width:12px;height:12px;opacity:.5}
    .view-btn{
      display:flex;align-items:center;gap:4px;
      background:none;border:1px solid var(--border);color:var(--muted);
      border-radius:var(--r);padding:5px 11px;font-size:12px;cursor:pointer;
      transition:all .14s;
    }
    .view-btn:hover{border-color:var(--orange);color:var(--orange);background:var(--og)}
    .view-btn svg{width:11px;height:11px}

    /* States */
    .loading-st{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:18px;padding:90px 0}
    .ring{width:42px;height:42px;border:3px solid var(--border);border-top-color:var(--orange);border-radius:50%;animation:spin .75s linear infinite}
    @@keyframes spin{to{transform:rotate(360deg)}}
    .loading-st p{font-family:var(--mono);font-size:10.5px;letter-spacing:2px;color:var(--muted)}
    .empty-st{display:flex;flex-direction:column;align-items:center;justify-content:center;gap:10px;padding:90px 0}
    .empty-st svg{width:48px;height:48px;color:var(--gray1)}
    .empty-st h3{font-family:var(--display);font-size:26px;letter-spacing:1px;color:var(--muted)}
    .empty-st p{font-size:13px;color:var(--muted)}
    .err-box{margin:24px;background:rgba(220,38,38,.05);border:1px solid rgba(220,38,38,.2);border-radius:var(--r);padding:16px 20px;display:flex;align-items:flex-start;gap:12px}
    .err-box svg{width:20px;height:20px;color:#dc2626;flex-shrink:0;margin-top:2px}
    .err-box p{font-size:13px;color:var(--dim)}
    .err-box strong{color:#dc2626}
    .retry-btn{margin-top:8px;background:rgba(220,38,38,.1);border:1px solid rgba(220,38,38,.25);color:#dc2626;padding:5px 14px;border-radius:var(--r);font-size:12px;cursor:pointer;display:block}

    /* Modal */
    .overlay{position:fixed;inset:0;background:rgba(0,0,0,.75);z-index:500;display:flex;align-items:center;justify-content:center;padding:24px;backdrop-filter:blur(8px);opacity:0;pointer-events:none;transition:opacity .22s}
    .overlay.open{opacity:1;pointer-events:auto}
    .modal{
      background:var(--card);border:1px solid var(--border);
      border-radius:var(--r2);width:100%;max-width:700px;max-height:90vh;
      overflow:hidden;display:flex;flex-direction:column;
      transform:translateY(22px) scale(.97);
      transition:transform .28s cubic-bezier(.22,.68,0,1.2);
      box-shadow:0 24px 64px rgba(0,0,0,.5);
    }
    .overlay.open .modal{transform:translateY(0) scale(1)}
    .modal-accent{height:4px;background:linear-gradient(90deg,var(--orange) 0%,var(--orange3) 100%);flex-shrink:0}
    .modal-img-wrap{height:240px;position:relative;overflow:hidden;flex-shrink:0;background:var(--card2)}
    .modal-img-wrap img{width:100%;height:100%;object-fit:cover;filter:brightness(.7) saturate(.8)}
    .modal-img-wrap::after{content:'';position:absolute;inset:0;background:linear-gradient(to bottom,rgba(30,30,30,.1) 0%,var(--card) 100%)}
    .modal-close-btn{
      position:absolute;top:12px;right:12px;z-index:10;
      background:rgba(30,30,30,.85);border:1px solid var(--border);
      border-radius:50%;width:34px;height:34px;display:flex;align-items:center;justify-content:center;
      cursor:pointer;color:var(--dim);transition:all .14s;
    }
    .modal-close-btn:hover{border-color:var(--orange);color:var(--orange)}
    .modal-close-btn svg{width:15px;height:15px}
    .modal-body{padding:0 26px 26px;overflow-y:auto;flex:1}
    .modal-name{font-family:var(--display);font-size:36px;letter-spacing:1.5px;color:var(--light);line-height:1;text-transform:capitalize;margin-bottom:10px}
    .modal-tags{display:flex;flex-wrap:wrap;gap:6px;margin-bottom:20px}
    .mtag{padding:3px 11px;border-radius:999px;font-size:10.5px;font-family:var(--mono);letter-spacing:.5px;border:1px solid;text-transform:capitalize}
    .mtag.m{color:var(--orange);border-color:var(--orange);background:var(--og)}
    .mtag.t{color:var(--dim);border-color:var(--border2)}
    .mtag.e{color:var(--dim);border-color:var(--border)}
    .modal-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:20px}
    .mstat{background:var(--card2);border:1px solid var(--border);border-radius:var(--r);padding:12px 14px}
    .mstat-lbl{font-family:var(--mono);font-size:9.5px;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:4px}
    .mstat-val{font-size:13.5px;font-weight:500;color:var(--text);text-transform:capitalize}
    .inst-lbl{font-family:var(--mono);font-size:9.5px;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:9px}
    .inst-text{font-size:14px;color:var(--dim);line-height:1.75}

    /* Responsive */
    @@media(max-width:900px){
      .intro-hero{grid-template-columns:1fr;max-height:none}
      .intro-right{height:240px;grid-template-rows:1fr}
      .intro-left{padding:60px 32px 40px}
      .intro-topbar{padding:0 32px}
      .intro-features{grid-template-columns:1fr 1fr}
    }
    @@media(max-width:680px){
      .app-shell{grid-template-columns:1fr}
      .sidebar{display:flex;overflow-x:auto;overflow-y:hidden;padding:6px 0;border-right:none;border-bottom:1px solid var(--border)}
      .sb-label,.sb-rule,.diff-wrap{display:none}
      .nav-item{flex-direction:column;padding:7px 12px;gap:3px;border-left:none;border-bottom:2px solid transparent;min-width:62px;align-items:center}
      .nav-item.active{border-bottom-color:var(--orange);border-left-color:transparent;background:transparent}
      .nav-thumb{width:34px;height:34px}
      .nav-name{font-size:10px;text-align:center}
      .nav-sub{display:none}
      .content{padding:14px}
      .intro-features{grid-template-columns:1fr}
    }
  </style>
</head>
<body>

<!-- ════════════════════════════════════════════════════════════
     INTRO PAGE
═════════════════════════════════════════════════════════════════ -->
<div id="introPage">

  <div class="intro-topbar">
    <div class="intro-logo">
      <div class="intro-logo-pip"></div>
      GYM<em>BUDDY</em>
    </div>
    <button class="nav-link" onclick="goToApp()">Browse Exercises →</button>
  </div>

  <div class="intro-hero">

    <!-- Left: copy -->
    <div class="intro-left">
      <div class="intro-left-content">
        <div class="intro-eyebrow">Exercise Library</div>
        <h1 class="intro-h1">TRAIN<br>LIKE YOU<br><em>MEAN IT</em></h1>
        <p class="intro-sub">
          Explore <strong>50+ exercises</strong> across every major muscle group.
          Filter by difficulty, type, and equipment — then get step-by-step
          instructions for every movement.
        </p>
        <div class="intro-stats">
          <div class="istat"><div class="istat-num">15<span>+</span></div><div class="istat-label">Muscle Groups</div></div>
          <div class="istat"><div class="istat-num">7<span>+</span></div><div class="istat-label">Exercise Types</div></div>
          <div class="istat"><div class="istat-num">3</div><div class="istat-label">Difficulty Levels</div></div>
        </div>
        <button class="cta-btn" onclick="goToApp()">
          Start Exploring
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5"><polyline points="9 18 15 12 9 6"/></svg>
        </button>
        <p class="intro-note">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
          Powered by API Ninjas Exercise Database
        </p>
      </div>
    </div>

    <!-- Right: compact mosaic -->
    <div class="intro-right">
      <div class="mosaic-img"><img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?w=500&q=80" alt="" /></div>
      <div class="mosaic-img"><img src="https://images.unsplash.com/photo-1571019614242-c5c5dee9f50b?w=360&q=80" alt="" /></div>
      <div class="mosaic-img"><img src="https://images.unsplash.com/photo-1583454110551-21f2fa2afe61?w=360&q=80" alt="" /></div>
      <div class="mosaic-img"><img src="https://images.unsplash.com/photo-1581009137042-c552e485697a?w=360&q=80" alt="" /></div>
      <div class="mosaic-img"><img src="https://images.unsplash.com/photo-1526506118085-60ce8714f8c5?w=360&q=80" alt="" /></div>
    </div>
  </div>

  <!-- Feature strip -->
  <div class="intro-features">
    <div class="feat">
      <div class="feat-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M6 4v16M18 4v16M3 8h3M18 8h3M3 16h3M18 16h3M6 8h12M6 16h12"/></svg></div>
      <div class="feat-title">50+ Exercises</div>
      <div class="feat-desc">Comprehensive database covering every major muscle group and movement pattern.</div>
    </div>
    <div class="feat">
      <div class="feat-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><circle cx="12" cy="12" r="3"/><path d="M12 1v4M12 19v4M4.22 4.22l2.83 2.83M16.95 16.95l2.83 2.83M1 12h4M19 12h4M4.22 19.78l2.83-2.83M16.95 7.05l2.83-2.83"/></svg></div>
      <div class="feat-title">Step-by-Step</div>
      <div class="feat-desc">Detailed instructions for every exercise so you always train with perfect form.</div>
    </div>
    <div class="feat">
      <div class="feat-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
      <div class="feat-title">Smart Filters</div>
      <div class="feat-desc">Filter by muscle group, exercise type, difficulty level, and equipment needed.</div>
    </div>
    <div class="feat">
      <div class="feat-icon"><svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8"><path d="M22 12h-4l-3 9L9 3l-3 9H2"/></svg></div>
      <div class="feat-title">3 Levels</div>
      <div class="feat-desc">Beginner, intermediate, and expert exercises clearly labeled for every fitness level.</div>
    </div>
  </div>

</div>

<!-- ════════════════════════════════════════════════════════════
     APP PAGE
═════════════════════════════════════════════════════════════════ -->
<div id="appPage">

  <div class="app-shell">

    <header class="app-topbar">
      <button class="back-btn" onclick="goToIntro()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="15 18 9 12 15 6"/></svg>
        Home
      </button>
      <div class="app-logo">GYM<em>BUDDY</em></div>
      <div class="search-wrap">
        <svg class="s-ico" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        <input type="text" id="searchInput" placeholder="Search exercises…" autocomplete="off" />
        <button class="s-clr" onclick="clearSearch()">×</button>
      </div>
      <div class="app-end">
        <div class="cnt-badge"><strong id="countNum">—</strong> results</div>
      </div>
    </header>

    <!-- Sidebar -->
    <nav class="sidebar">
      <div class="sb-label">Muscle Group</div>
      <div class="nav-item active" data-muscle="lower_back" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1583454110551-21f2fa2afe61?w=80&h=80&fit=crop&q=70" alt="back" /></div>
        <div class="nav-info"><span class="nav-name">Back</span><span class="nav-sub">Lats · Rhomboids</span></div>
      </div>
      <div class="nav-item" data-muscle="shoulders" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1571019614242-c5c5dee9f50b?w=80&h=80&fit=crop&q=70" alt="shoulders" /></div>
        <div class="nav-info"><span class="nav-name">Shoulders</span><span class="nav-sub">Deltoids</span></div>
      </div>
      <div class="nav-item" data-muscle="biceps" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1581009137042-c552e485697a?w=80&h=80&fit=crop&q=70" alt="biceps" /></div>
        <div class="nav-info"><span class="nav-name">Biceps</span><span class="nav-sub">Upper Arms</span></div>
      </div>
      <div class="nav-item" data-muscle="triceps" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1581009146145-b5ef050c2e1e?w=80&h=80&fit=crop&q=70" alt="triceps" /></div>
        <div class="nav-info"><span class="nav-name">Triceps</span><span class="nav-sub">Upper Arms</span></div>
      </div>
      <div class="nav-item" data-muscle="forearms" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1526506118085-60ce8714f8c5?w=80&h=80&fit=crop&q=70" alt="forearms" /></div>
        <div class="nav-info"><span class="nav-name">Forearms</span><span class="nav-sub">Lower Arms</span></div>
      </div>
      <div class="nav-item" data-muscle="chest" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1534438327276-14e5300c3a48?w=80&h=80&fit=crop&q=70" alt="chest" /></div>
        <div class="nav-info"><span class="nav-name">Chest</span><span class="nav-sub">Pectorals</span></div>
      </div>
      <div class="nav-item" data-muscle="quadriceps" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1574680178050-55c6a6a96e0a?w=80&h=80&fit=crop&q=70" alt="quads" /></div>
        <div class="nav-info"><span class="nav-name">Quadriceps</span><span class="nav-sub">Upper Legs</span></div>
      </div>
      <div class="nav-item" data-muscle="hamstrings" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1434608519344-49d77a699e1d?w=80&h=80&fit=crop&q=70" alt="hamstrings" /></div>
        <div class="nav-info"><span class="nav-name">Hamstrings</span><span class="nav-sub">Upper Legs</span></div>
      </div>
      <div class="nav-item" data-muscle="calves" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1476480862126-209bfaa8edc8?w=80&h=80&fit=crop&q=70" alt="calves" /></div>
        <div class="nav-info"><span class="nav-name">Calves</span><span class="nav-sub">Lower Legs</span></div>
      </div>
      <div class="nav-item" data-muscle="glutes" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1571019613454-1cb2f99b2d8b?w=80&h=80&fit=crop&q=70" alt="glutes" /></div>
        <div class="nav-info"><span class="nav-name">Glutes</span><span class="nav-sub">Hip Extensors</span></div>
      </div>
      <div class="nav-item" data-muscle="abdominals" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1571019613914-85f342c6a11e?w=80&h=80&fit=crop&q=70" alt="abs" /></div>
        <div class="nav-info"><span class="nav-name">Abdominals</span><span class="nav-sub">Core · Waist</span></div>
      </div>
      <div class="nav-item" data-muscle="lats" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1598971457999-ca4ef48a9a71?w=80&h=80&fit=crop&q=70" alt="lats" /></div>
        <div class="nav-info"><span class="nav-name">Lats</span><span class="nav-sub">Latissimus Dorsi</span></div>
      </div>
      <div class="nav-item" data-muscle="traps" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1532029837206-abbe2b7620e3?w=80&h=80&fit=crop&q=70" alt="traps" /></div>
        <div class="nav-info"><span class="nav-name">Traps</span><span class="nav-sub">Trapezius</span></div>
      </div>
      <div class="nav-item" data-muscle="neck" data-type="" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1549060279-7e168fcee0c2?w=80&h=80&fit=crop&q=70" alt="neck" /></div>
        <div class="nav-info"><span class="nav-name">Neck</span><span class="nav-sub">Cervical</span></div>
      </div>
      <div class="nav-item" data-muscle="" data-type="cardio" onclick="selectNav(this)">
        <div class="nav-thumb"><img src="https://images.unsplash.com/photo-1476480862126-209bfaa8edc8?w=80&h=80&fit=crop&q=70" alt="cardio" /></div>
        <div class="nav-info"><span class="nav-name">Cardio</span><span class="nav-sub">Endurance</span></div>
      </div>

      <div class="sb-rule"></div>
      <div class="sb-label">Difficulty</div>
      <div class="diff-wrap">
        <button class="diff-pill active" data-diff="" onclick="selectDiff(this)"><span class="dd a"></span>All Levels</button>
        <button class="diff-pill" data-diff="beginner" onclick="selectDiff(this)"><span class="dd b"></span>Beginner</button>
        <button class="diff-pill" data-diff="intermediate" onclick="selectDiff(this)"><span class="dd i"></span>Intermediate</button>
        <button class="diff-pill" data-diff="expert" onclick="selectDiff(this)"><span class="dd e"></span>Expert</button>
      </div>
    </nav>

    <!-- Main -->
    <main class="main-panel">
      <div class="main-hero">
        <div class="hero-img-strip">
          <img id="heroImg" src="https://images.unsplash.com/photo-1583454110551-21f2fa2afe61?w=1200&q=80" alt="" />
        </div>
        <div class="hero-content">
          <div class="hero-eyebrow" id="heroEye">Muscle Group</div>
          <h1 class="hero-title" id="heroTitle">Back <em>Exercises</em></h1>
          <p class="hero-meta" id="heroMeta">Loading exercises…</p>
        </div>
      </div>

      <div class="chip-row">
        <span class="chip-row-lbl">Type —</span>
        <div id="typeChips" style="display:flex;gap:7px"></div>
      </div>

      <div class="content" id="contentArea">
        <div class="loading-st" id="loadingState"><div class="ring"></div><p>LOADING EXERCISES</p></div>
        <div class="ex-grid" id="exGrid" style="display:none"></div>
        <div class="empty-st" id="emptyState" style="display:none">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
          <h3>No Results</h3><p>Try adjusting your filters.</p>
        </div>
      </div>
    </main>
  </div>
</div>

<!-- Modal -->
<div class="overlay" id="overlay" onclick="closeOutside(event)">
  <div class="modal">
    <div class="modal-accent"></div>
    <div class="modal-img-wrap">
      <button class="modal-close-btn" onclick="closeModal()">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
      <img id="mImg" src="" alt="" />
    </div>
    <div class="modal-body">
      <h2 class="modal-name" id="mName"></h2>
      <div class="modal-tags" id="mTags"></div>
      <div class="modal-grid" id="mGrid"></div>
      <div class="inst-lbl">Instructions</div>
      <p class="inst-text" id="mInst"></p>
    </div>
  </div>
</div>

<script>
  // ─── EXERCISE-SPECIFIC PHOTO ─────────────────────────────────
  // Maps equipment type → curated Unsplash photo IDs.
  // We pick a photo by hashing the exercise name so each card
  // gets a consistent, unique image that won't repeat within a page.
  const EQUIP_PHOTOS = {
    barbell:          ['photo-1534438327276-14e5300c3a48','photo-1583454110551-21f2fa2afe61','photo-1541534741688-6078c6bfb5c5','photo-1517836357463-d25dfeac3438','photo-1526506118085-60ce8714f8c5'],
    dumbbell:         ['photo-1581009137042-c552e485697a','photo-1571019614242-c5c5dee9f50b','photo-1526506118085-60ce8714f8c5','photo-1577221084712-45b0445d2b00','photo-1581009146145-b5ef050c2e1e'],
    'ez barbell':     ['photo-1534438327276-14e5300c3a48','photo-1541534741688-6078c6bfb5c5','photo-1583454110551-21f2fa2afe61','photo-1517836357463-d25dfeac3438','photo-1526506118085-60ce8714f8c5'],
    cable:            ['photo-1532029837206-abbe2b7620e3','photo-1598971457999-ca4ef48a9a71','photo-1571019614242-c5c5dee9f50b','photo-1549060279-7e168fcee0c2','photo-1583454110551-21f2fa2afe61'],
    machine:          ['photo-1549060279-7e168fcee0c2','photo-1598971457999-ca4ef48a9a71','photo-1541534741688-6078c6bfb5c5','photo-1571019614242-c5c5dee9f50b','photo-1532029837206-abbe2b7620e3'],
    'leverage machine':['photo-1549060279-7e168fcee0c2','photo-1532029837206-abbe2b7620e3','photo-1598971457999-ca4ef48a9a71','photo-1583454110551-21f2fa2afe61','photo-1571019614242-c5c5dee9f50b'],
    band:             ['photo-1571019613914-85f342c6a11e','photo-1476480862126-209bfaa8edc8','photo-1434608519344-49d77a699e1d','photo-1571019613454-1cb2f99b2d8b','photo-1574680178050-55c6a6a96e0a'],
    kettlebell:       ['photo-1517836357463-d25dfeac3438','photo-1534438327276-14e5300c3a48','photo-1526506118085-60ce8714f8c5','photo-1581009137042-c552e485697a','photo-1583454110551-21f2fa2afe61'],
    'body weight':    ['photo-1571019613914-85f342c6a11e','photo-1476480862126-209bfaa8edc8','photo-1574680178050-55c6a6a96e0a','photo-1434608519344-49d77a699e1d','photo-1571019613454-1cb2f99b2d8b'],
    bodyweight:       ['photo-1571019613914-85f342c6a11e','photo-1476480862126-209bfaa8edc8','photo-1574680178050-55c6a6a96e0a','photo-1434608519344-49d77a699e1d','photo-1571019613454-1cb2f99b2d8b'],
    'medicine ball':  ['photo-1517836357463-d25dfeac3438','photo-1571019613914-85f342c6a11e','photo-1476480862126-209bfaa8edc8','photo-1574680178050-55c6a6a96e0a','photo-1434608519344-49d77a699e1d'],
    cardio:           ['photo-1476480862126-209bfaa8edc8','photo-1571019613454-1cb2f99b2d8b','photo-1434608519344-49d77a699e1d','photo-1574680178050-55c6a6a96e0a','photo-1535914254981-b5012eebbd15'],
    default:          ['photo-1583454110551-21f2fa2afe61','photo-1571019614242-c5c5dee9f50b','photo-1581009137042-c552e485697a','photo-1534438327276-14e5300c3a48','photo-1532029837206-abbe2b7620e3'],
  };

  function getExercisePhoto(ex) {
    const equip = (Array.isArray(ex.equipment) ? ex.equipment[0] : ex.equipment || '').toLowerCase().trim();
    const pool  = EQUIP_PHOTOS[equip] || EQUIP_PHOTOS.default;
    // Seed from exercise name so same exercise = same photo every render
    const seed  = (ex.name || '').split('').reduce((a, c) => a + c.charCodeAt(0), 0);
    const id    = pool[seed % pool.length];
    return `https://images.unsplash.com/${id}?w=600&h=380&fit=crop&q=80`;
  }

  // Hero banner — muscle-group keyed fixed photos for stable banner
  const HERO_PHOTOS = {
    lower_back:  'photo-1583454110551-21f2fa2afe61',
    shoulders:   'photo-1571019614242-c5c5dee9f50b',
    biceps:      'photo-1581009137042-c552e485697a',
    triceps:     'photo-1581009146145-b5ef050c2e1e',
    forearms:    'photo-1526506118085-60ce8714f8c5',
    chest:       'photo-1534438327276-14e5300c3a48',
    quadriceps:  'photo-1574680178050-55c6a6a96e0a',
    hamstrings:  'photo-1434608519344-49d77a699e1d',
    calves:      'photo-1476480862126-209bfaa8edc8',
    glutes:      'photo-1571019613454-1cb2f99b2d8b',
    abdominals:  'photo-1571019613914-85f342c6a11e',
    lats:        'photo-1598971457999-ca4ef48a9a71',
    traps:       'photo-1532029837206-abbe2b7620e3',
    neck:        'photo-1549060279-7e168fcee0c2',
    cardio:      'photo-1476480862126-209bfaa8edc8',
  };
  function getHeroPhoto(muscle) {
    const id = HERO_PHOTOS[muscle] || 'photo-1583454110551-21f2fa2afe61';
    return `https://images.unsplash.com/${id}?w=1400&h=260&fit=crop&q=85`;
  }

  // ─── CONFIG ───────────────────────────────────────────────────
  const PROXY = '/Exercise/ByMuscle';
  const TYPES = ['cardio','olympic_weightlifting','plyometrics','powerlifting','strength','stretching','strongman'];

  // ─── STATE ────────────────────────────────────────────────────
  let currentMuscle = 'lower_back', currentType = '';
  let activeDiff = '', activeType = '', searchQuery = '';
  let allFetched = [], appInitialized = false;

  // ─── NAV ──────────────────────────────────────────────────────
  function goToApp() {
    document.getElementById('introPage').style.display = 'none';
    const ap = document.getElementById('appPage');
    ap.style.display = 'flex'; ap.classList.add('active');
    if (!appInitialized) {
      appInitialized = true;
      buildTypeChips(); loadExercises();
      document.getElementById('searchInput').addEventListener('input', e => {
        searchQuery = e.target.value.toLowerCase(); applyAndRender();
      });
    }
  }
  function goToIntro() {
    document.getElementById('appPage').style.display = 'none';
    document.getElementById('introPage').style.display = 'flex';
  }

  function buildTypeChips() {
    const w = document.getElementById('typeChips');
    TYPES.forEach(t => {
      const b = document.createElement('button');
      b.className = 'chip'; b.textContent = t.replace(/_/g,' '); b.dataset.t = t;
      b.onclick = () => toggleType(b, t); w.appendChild(b);
    });
  }
  function toggleType(btn, t) {
    if (activeType === t) { activeType = ''; btn.classList.remove('active'); }
    else { document.querySelectorAll('.chip').forEach(c=>c.classList.remove('active')); activeType = t; btn.classList.add('active'); }
    applyAndRender();
  }

  function selectNav(el) {
    document.querySelectorAll('.nav-item').forEach(n=>n.classList.remove('active'));
    el.classList.add('active');
    currentMuscle = el.dataset.muscle; currentType = el.dataset.type;
    activeType = ''; document.querySelectorAll('.chip').forEach(c=>c.classList.remove('active'));
    const nm = el.querySelector('.nav-name').textContent;
    const sub = el.querySelector('.nav-sub').textContent;
    document.getElementById('heroEye').textContent = sub || 'Muscle Group';
    document.getElementById('heroTitle').innerHTML = nm + ' <em>Exercises</em>';
    document.getElementById('heroImg').src = getHeroPhoto(currentMuscle);
    loadExercises();
  }

  function selectDiff(btn) {
    document.querySelectorAll('.diff-pill').forEach(p=>p.classList.remove('active'));
    btn.classList.add('active'); activeDiff = btn.dataset.diff; loadExercises();
  }

  // ─── LOAD  ────────────────────────────────────────────────────
  // FIX: ignore 502/non-ok responses silently — API returns 502
  // when offset exceeds available results (not a real error).
  async function loadExercises() {
    showLoading(); allFetched = [];
    await Promise.all([0,10,20,30,40].map(o => fetchBatch(o)));
    applyAndRender();
  }

  async function fetchBatch(offset) {
    try {
      const p = new URLSearchParams();
      if (currentMuscle) p.set('muscle', currentMuscle);
      if (currentType)   p.set('type',   currentType);
      if (activeDiff)    p.set('difficulty', activeDiff);
      p.set('offset', offset);
      const res = await fetch(`${PROXY}?${p}`);
      // ← silently skip non-OK responses (402/429/502 from offset overrun)
      if (!res.ok) return;
      const d = await res.json();
      if (Array.isArray(d)) allFetched.push(...d);
    } catch { /* network error — silently skip */ }
  }

  // ─── FILTER + RENDER ──────────────────────────────────────────
  function applyAndRender() {
    const filtered = allFetched.filter(ex => {
      if (activeType && ex.type !== activeType) return false;
      if (searchQuery) {
        const h = [ex.name,ex.muscle,ex.type,ex.difficulty,ex.equipment].join(' ').toLowerCase();
        if (!h.includes(searchQuery)) return false;
      }
      return true;
    });
    document.getElementById('countNum').textContent = filtered.length;
    document.getElementById('heroMeta').textContent =
      `${filtered.length} exercise${filtered.length!==1?'s':''} found`;
    renderGrid(filtered);
  }

  function renderGrid(data) {
    const grid = document.getElementById('exGrid');
    const empty = document.getElementById('emptyState');
    document.getElementById('loadingState').style.display = 'none';
    if (!data.length) { grid.style.display='none'; empty.style.display='flex'; return; }
    empty.style.display='none'; grid.style.display='grid'; grid.innerHTML='';
    data.forEach((ex, i) => grid.appendChild(buildCard(ex, i)));
  }

  // ─── BUILD CARD ───────────────────────────────────────────────
  function buildCard(ex, i) {
    const card = document.createElement('div');
    card.className = 'ex-card';
    card.style.animationDelay = Math.min(i*.05,.7)+'s';
    const imgSrc = getExercisePhoto(ex);
    const diffCls = (ex.difficulty||'').toLowerCase();
    const equipArr = Array.isArray(ex.equipment)?ex.equipment:(ex.equipment?[ex.equipment]:[]);
    const equipStr = equipArr.length?equipArr.join(', '):'Bodyweight';

    card.innerHTML = `
      <div class="card-accent"></div>
      <div class="card-img-wrap">
        <img src="${imgSrc}" alt="${ex.name}" loading="lazy" />
        ${diffCls?`<div class="diff-badge ${diffCls}">${cap(ex.difficulty)}</div>`:''}
        <div class="type-badge">${(ex.type||'').replace(/_/g,' ')}</div>
      </div>
      <div class="card-body">
        <div class="card-name">${cap(ex.name)}</div>
        <div class="card-muscle"><div class="muscle-pip"></div><span>${cap(ex.muscle||'General')}</span></div>
        <p class="card-desc">${ex.instructions||'No description available.'}</p>
      </div>
      <div class="card-foot">
        <span class="card-equip">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M6 4v16M18 4v16M3 8h3M18 8h3M3 16h3M18 16h3M6 8h12M6 16h12"/></svg>
          ${equipStr}
        </span>
        <button class="view-btn">View <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="9 18 15 12 9 6"/></svg></button>
      </div>`;

    card.onclick = () => openModal(ex, imgSrc);
    card.querySelector('.view-btn').onclick = e => { e.stopPropagation(); openModal(ex, imgSrc); };
    return card;
  }

  // ─── MODAL ────────────────────────────────────────────────────
  function openModal(ex, imgSrc) {
    document.getElementById('mImg').src = imgSrc;
    document.getElementById('mName').textContent = cap(ex.name||'');
    const dc = (ex.difficulty||'').toLowerCase();
    const dcol = {beginner:'#16a34a',intermediate:'#FF8B10',expert:'#dc2626'}[dc]||'#6b7480';
    const eq = Array.isArray(ex.equipment)?ex.equipment:(ex.equipment?[ex.equipment]:[]);
    document.getElementById('mTags').innerHTML =
      `<span class="mtag m">${cap(ex.muscle||'—')}</span>
       <span class="mtag t">${(ex.type||'—').replace(/_/g,' ')}</span>
       ${eq.map(e=>`<span class="mtag e">${cap(e)}</span>`).join('')}
       ${dc?`<span class="mtag" style="color:${dcol};border-color:${dcol};background:${dcol}18">${cap(ex.difficulty)}</span>`:''}`;
    document.getElementById('mGrid').innerHTML =
      `<div class="mstat"><div class="mstat-lbl">Muscle</div><div class="mstat-val">${cap(ex.muscle||'—')}</div></div>
       <div class="mstat"><div class="mstat-lbl">Type</div><div class="mstat-val">${(ex.type||'—').replace(/_/g,' ')}</div></div>
       <div class="mstat"><div class="mstat-lbl">Difficulty</div><div class="mstat-val" style="color:${dcol}">${cap(ex.difficulty||'—')}</div></div>`;
    document.getElementById('mInst').textContent = ex.instructions||'No instructions available.';
    document.getElementById('overlay').classList.add('open');
    document.body.style.overflow = 'hidden';
  }
  function closeModal() { document.getElementById('overlay').classList.remove('open'); document.body.style.overflow=''; }
  function closeOutside(e) { if(e.target===document.getElementById('overlay')) closeModal(); }
  document.addEventListener('keydown', e => { if(e.key==='Escape') closeModal(); });

  function cap(s) { return s?s.replace(/\b\w/g,c=>c.toUpperCase()):''; }
  function showLoading() {
    document.getElementById('loadingState').style.display='flex';
    document.getElementById('exGrid').style.display='none';
    document.getElementById('emptyState').style.display='none';
    document.querySelectorAll('.err-box').forEach(e=>e.remove());
  }
  function clearSearch() { document.getElementById('searchInput').value=''; searchQuery=''; applyAndRender(); }
</script>
</body>
</html>
